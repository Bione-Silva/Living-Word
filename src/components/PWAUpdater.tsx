// PWA temporarily disabled — vite-plugin-pwa removed to reset Vercel stale build cache.
// Restore full implementation (with virtual:pwa-register/react) after first successful Vercel build.
export function PWAUpdater() {
  return null;
}
